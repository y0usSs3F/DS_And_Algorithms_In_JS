const letters = ['d', 'c', 'a', 'x', 't']
const basket = [2, 65, 34, 2, 3, 20]

console.log(letters.sort());
sorted_numbers = basket.sort(function (a, b) { return a - b; });
console.log(sorted_numbers);
